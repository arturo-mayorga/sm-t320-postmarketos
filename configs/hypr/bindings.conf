# Omarchy keybindings, ported to plain Hyprland for the tablet.
# Same keys as the laptop; the omarchy-* commands are replaced by small
# scripts in ~/.local/bin (tab-*) or the equivalent Hyprland dispatcher.

$mod = SUPER
$terminal = alacritty
$menu = fuzzel

# ---- Applications / menus
bind = $mod, RETURN, exec, $terminal
bind = $mod ALT, RETURN, exec, $terminal -e tmux
bind = $mod, SPACE, exec, $menu
bind = $mod ALT, SPACE, exec, $menu
bind = $mod, ESCAPE, exec, tab-system-menu
bind = $mod, K, exec, tab-keybindings
bind = $mod ALT, K, exec, pkill -SIGRTMIN wvkbd-mobintl          # on-screen keyboard
bind = $mod CTRL, L, exec, hyprlock
bind = $mod CTRL, A, exec, $terminal -e alsamixer

# ---- Windows
bind = $mod, W, killactive
bind = CTRL ALT, DELETE, exec, tab-close-all
bind = $mod, F, fullscreen, 0
bind = $mod ALT, F, fullscreen, 1
bind = $mod, T, togglefloating
bind = $mod, J, togglesplit
bind = $mod, O, exec, hyprctl dispatch togglefloating && hyprctl dispatch pin
bind = $mod, G, togglegroup
bind = $mod, BACKSPACE, exec, tab-opacity-toggle

# ---- Clipboard (terminal-aware, like Omarchy's universal copy/paste)
bind = $mod, C, exec, tab-clip copy
bind = $mod, V, exec, tab-clip paste
bind = $mod, X, exec, tab-clip cut

# ---- Capture
bind = , PRINT, exec, tab-screenshot region
bind = SHIFT, PRINT, exec, tab-screenshot output
bind = $mod, PRINT, exec, pkill hyprpicker || hyprpicker -a

# ---- Workspaces
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9
bind = $mod SHIFT, 0, movetoworkspace, 10
bind = $mod SHIFT ALT, 1, movetoworkspacesilent, 1
bind = $mod SHIFT ALT, 2, movetoworkspacesilent, 2
bind = $mod SHIFT ALT, 3, movetoworkspacesilent, 3
bind = $mod SHIFT ALT, 4, movetoworkspacesilent, 4
bind = $mod SHIFT ALT, 5, movetoworkspacesilent, 5
bind = $mod SHIFT ALT, 6, movetoworkspacesilent, 6
bind = $mod SHIFT ALT, 7, movetoworkspacesilent, 7
bind = $mod SHIFT ALT, 8, movetoworkspacesilent, 8
bind = $mod SHIFT ALT, 9, movetoworkspacesilent, 9
bind = $mod SHIFT ALT, 0, movetoworkspacesilent, 10
bind = $mod, TAB, workspace, e+1
bind = $mod SHIFT, TAB, workspace, e-1
bind = $mod CTRL, TAB, workspace, previous
bind = $mod, S, togglespecialworkspace, scratchpad
bind = $mod ALT, S, movetoworkspacesilent, special:scratchpad
bind = $mod, mouse_down, workspace, e+1
bind = $mod, mouse_up, workspace, e-1

# ---- Focus / move
bind = $mod, LEFT, movefocus, l
bind = $mod, RIGHT, movefocus, r
bind = $mod, UP, movefocus, u
bind = $mod, DOWN, movefocus, d
bind = ALT, TAB, cyclenext
bind = SHIFT ALT, TAB, cyclenext, prev
bind = $mod SHIFT, LEFT, swapwindow, l
bind = $mod SHIFT, RIGHT, swapwindow, r
bind = $mod SHIFT, UP, swapwindow, u
bind = $mod SHIFT, DOWN, swapwindow, d
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

# ---- Resize (Omarchy: minus/equal = width, shift = height; alt = a little, ctrl = a lot)
binde = $mod, MINUS, resizeactive, -100 0
binde = $mod, EQUAL, resizeactive, 100 0
binde = $mod SHIFT, MINUS, resizeactive, 0 -100
binde = $mod SHIFT, EQUAL, resizeactive, 0 100
binde = $mod ALT, MINUS, resizeactive, -20 0
binde = $mod ALT, EQUAL, resizeactive, 20 0
binde = $mod SHIFT ALT, MINUS, resizeactive, 0 -20
binde = $mod SHIFT ALT, EQUAL, resizeactive, 0 20
binde = $mod CTRL, MINUS, resizeactive, -400 0
binde = $mod CTRL, EQUAL, resizeactive, 400 0
binde = $mod SHIFT CTRL, MINUS, resizeactive, 0 -400
binde = $mod SHIFT CTRL, EQUAL, resizeactive, 0 400

# ---- Notifications (mako)
bind = $mod, COMMA, exec, makoctl dismiss
bind = $mod SHIFT, COMMA, exec, makoctl dismiss -a
bind = $mod ALT, COMMA, exec, makoctl invoke
bind = $mod SHIFT ALT, COMMA, exec, makoctl restore
bind = $mod CTRL, COMMA, exec, makoctl mode -t do-not-disturb

# ---- Media keys
bindel = , XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = , XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindl  = , XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindl  = , XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindl  = , XF86AudioPlay, exec, playerctl play-pause
bindl  = , XF86AudioNext, exec, playerctl next
bindl  = , XF86AudioPrev, exec, playerctl previous
