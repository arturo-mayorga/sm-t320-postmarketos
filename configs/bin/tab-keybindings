#!/bin/sh
# Show the bindings file in fuzzel, like Omarchy's keybindings menu.
grep -E '^bind[elm]* *=' ~/.config/hypr/bindings.conf | sed -E 's/^bind[elm]* *= *//; s/\$mod/SUPER/g; s/, */ + /; s/, */  →  /' | fuzzel --dmenu -p "Keys: " -l 25 -w 70 >/dev/null
