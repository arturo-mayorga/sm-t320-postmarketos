# Start the desktop on the first console, once, after autologin.
export XDG_RUNTIME_DIR=/run/user/$(id -u)
if [ -z "$WAYLAND_DISPLAY" ] && [ "$(tty)" = "/dev/tty1" ]; then
	exec dbus-run-session start-hyprland
fi
